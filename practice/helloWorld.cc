#include <iostream>
// to avoid having to do std:: in front of cout and endl, we can use
using std::cout;
using std::endl;
// or for all things to use std
// using namespace std;

int main() {
    // std::cout << "Hello world!" << std::endl;
    cout << "Hello world! (but better)" << endl;
    return 0;
}
